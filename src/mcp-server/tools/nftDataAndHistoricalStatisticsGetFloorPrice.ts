/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftDataAndHistoricalStatisticsGetFloorPrice } from "../../funcs/nftDataAndHistoricalStatisticsGetFloorPrice.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetFloorPriceRequest$inboundSchema,
};

export const tool$nftDataAndHistoricalStatisticsGetFloorPrice: ToolDefinition<
  typeof args
> = {
  name: "NFT-data-and-historical-statistics-get-floor-price",
  description: `Get NFT Collection floor price

Get historical statistics about Collection's NFT floor price`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nftDataAndHistoricalStatisticsGetFloorPrice(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
