/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftActivitiesGetActivitiesByItem } from "../../funcs/nftActivitiesGetActivitiesByItem.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetActivitiesByItemRequest$inboundSchema,
};

export const tool$nftActivitiesGetActivitiesByItem: ToolDefinition<
  typeof args
> = {
  name: "NFT-activities-get-activities-by-item",
  description: `Get NFT Activities

Returns Activities related to specified NFT and sorted by date. This API is deprecated in favor of \`Search Activities\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nftActivitiesGetActivitiesByItem(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
