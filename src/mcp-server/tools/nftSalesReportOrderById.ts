/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftSalesReportOrderById } from "../../funcs/nftSalesReportOrderById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ReportOrderByIdRequest$inboundSchema,
};

export const tool$nftSalesReportOrderById: ToolDefinition<typeof args> = {
  name: "NFT-sales-report-order-by-id",
  description: `Report Order

Report Error Order`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nftSalesReportOrderById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
