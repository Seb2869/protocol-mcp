/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftOwnershipsGetOwnershipsByIds } from "../../funcs/nftOwnershipsGetOwnershipsByIds.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.OwnershipIds$inboundSchema,
};

export const tool$nftOwnershipsGetOwnershipsByIds: ToolDefinition<typeof args> =
  {
    name: "NFT-ownerships-get-ownerships-by-ids",
    description: `Get NFT Ownerships by Ids

Returns Ownerships by specified list of Ids`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await nftOwnershipsGetOwnershipsByIds(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
