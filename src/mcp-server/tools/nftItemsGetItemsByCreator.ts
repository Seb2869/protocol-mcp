/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftItemsGetItemsByCreator } from "../../funcs/nftItemsGetItemsByCreator.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetItemsByCreatorRequest$inboundSchema,
};

export const tool$nftItemsGetItemsByCreator: ToolDefinition<typeof args> = {
  name: "NFT-items-get-items-by-creator",
  description: `Get NFT created by user

Returns list of NFTs created by specified user and sorted by \`last updated\` date`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nftItemsGetItemsByCreator(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
