/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftSalesPrepareOrderTransaction } from "../../funcs/nftSalesPrepareOrderTransaction.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PrepareOrderTransactionRequest$inboundSchema,
};

export const tool$nftSalesPrepareOrderTransaction: ToolDefinition<typeof args> =
  {
    name: "NFT-sales-prepare-order-transaction",
    description: `Prepare order transaction

Prepare all required data to match given order on the blockchain`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await nftSalesPrepareOrderTransaction(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
