/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { userBalancesGetBalance } from "../../funcs/userBalancesGetBalance.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetBalanceRequest$inboundSchema,
};

export const tool$userBalancesGetBalance: ToolDefinition<typeof args> = {
  name: "user-balances-get-balance",
  description: `Get balance

Return user's balance of specified currency`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await userBalancesGetBalance(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
