/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftSalesUpsertOrder } from "../../funcs/nftSalesUpsertOrder.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.OrderForm$inboundSchema,
};

export const tool$nftSalesUpsertOrder: ToolDefinition<typeof args> = {
  name: "NFT-sales-upsert-order",
  description: `Create or update Order

Create or update off-chain Order (supported only for some blockchains)`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nftSalesUpsertOrder(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
