/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftSalesGetOrderBidsByItem } from "../../funcs/nftSalesGetOrderBidsByItem.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOrderBidsByItemRequest$inboundSchema,
};

export const tool$nftSalesGetOrderBidsByItem: ToolDefinition<typeof args> = {
  name: "NFT-sales-get-order-bids-by-item",
  description: `Get bid Orders for NFT

Returns bid Orders created for specified NFT and sorted by price in USD (expensive first)`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nftSalesGetOrderBidsByItem(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
