/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftSalesGetSellOrdersByMaker } from "../../funcs/nftSalesGetSellOrdersByMaker.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSellOrdersByMakerRequest$inboundSchema,
};

export const tool$nftSalesGetSellOrdersByMaker: ToolDefinition<typeof args> = {
  name: "NFT-sales-get-sell-orders-by-maker",
  description: `Get user's sell Orders

Returns sell NFT Sales created by specified user and sorted by \`last update\` date`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nftSalesGetSellOrdersByMaker(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
