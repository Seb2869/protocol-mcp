/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { currenciesAndRatesGetUsdRate } from "../../funcs/currenciesAndRatesGetUsdRate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetUsdRateRequest$inboundSchema,
};

export const tool$currenciesAndRatesGetUsdRate: ToolDefinition<typeof args> = {
  name: "currencies-and-rates-get-usd-rate",
  description: `Get USD rate

Get currency USD rate by currency blockchain's address`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await currenciesAndRatesGetUsdRate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
