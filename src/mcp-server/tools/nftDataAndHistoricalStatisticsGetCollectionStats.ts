/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftDataAndHistoricalStatisticsGetCollectionStats } from "../../funcs/nftDataAndHistoricalStatisticsGetCollectionStats.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCollectionStatsRequest$inboundSchema,
};

export const tool$nftDataAndHistoricalStatisticsGetCollectionStats:
  ToolDefinition<typeof args> = {
    name: "NFT-data-and-historical-statistics-get-collection-stats",
    description: `Get NFT Collection stats

Get general statistics about Collection`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await nftDataAndHistoricalStatisticsGetCollectionStats(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
