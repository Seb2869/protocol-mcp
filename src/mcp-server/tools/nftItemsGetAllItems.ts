/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftItemsGetAllItems } from "../../funcs/nftItemsGetAllItems.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAllItemsRequest$inboundSchema,
};

export const tool$nftItemsGetAllItems: ToolDefinition<typeof args> = {
  name: "NFT-items-get-all-items",
  description: `Get all NFTs

Returns all NFT Items in accordance with specified filters and sorted by \`last updated\` date`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nftItemsGetAllItems(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
