/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftSalesGetOrderBidsByMaker } from "../../funcs/nftSalesGetOrderBidsByMaker.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOrderBidsByMakerRequest$inboundSchema,
};

export const tool$nftSalesGetOrderBidsByMaker: ToolDefinition<typeof args> = {
  name: "NFT-sales-get-order-bids-by-maker",
  description: `Get user's bid Orders

Returns bid Orders created by specified user and sorted by \`last update\` date`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nftSalesGetOrderBidsByMaker(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
