/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftSalesGetAllSync } from "../../funcs/nftSalesGetAllSync.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAllSyncRequest$inboundSchema,
};

export const tool$nftSalesGetAllSync: ToolDefinition<typeof args> = {
  name: "NFT-sales-get-all-sync",
  description: `Get all Orders (for sync)

Returns all sales & transfers in accordance with specified filters and sorted by \`db updated\` date. During internal updates (like migrations) Orders can be updated for technical reasons. In such case, \`last update\` date won't be changed. If you want to store Orders in your own storage and keep it synced, use this method.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nftSalesGetAllSync(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
