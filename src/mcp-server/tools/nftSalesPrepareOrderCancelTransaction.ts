/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftSalesPrepareOrderCancelTransaction } from "../../funcs/nftSalesPrepareOrderCancelTransaction.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PrepareOrderCancelTransactionRequest$inboundSchema,
};

export const tool$nftSalesPrepareOrderCancelTransaction: ToolDefinition<
  typeof args
> = {
  name: "NFT-sales-prepare-order-cancel-transaction",
  description: `Prepare order cancel transaction

Prepare all required data to cancel given order on the blockchain`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nftSalesPrepareOrderCancelTransaction(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
