/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftOwnershipsGetOwnershipsByCollection } from "../../funcs/nftOwnershipsGetOwnershipsByCollection.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOwnershipsByCollectionRequest$inboundSchema,
};

export const tool$nftOwnershipsGetOwnershipsByCollection: ToolDefinition<
  typeof args
> = {
  name: "NFT-ownerships-get-ownerships-by-collection",
  description: `Get NFT Collection's Ownerships

Returns list of NFTs Ownerships from specified collection and sorted by \`last updated\` date`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nftOwnershipsGetOwnershipsByCollection(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
