/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { searchAPISearchActivities } from "../../funcs/searchAPISearchActivities.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.ActivitySearchRequest$inboundSchema,
};

export const tool$searchAPISearchActivities: ToolDefinition<typeof args> = {
  name: "search-API-search-activities",
  description: `Search Activities

Advanced search returns Activities satisfying provided filter`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await searchAPISearchActivities(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
