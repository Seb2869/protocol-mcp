/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftSalesGetOrderById } from "../../funcs/nftSalesGetOrderById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOrderByIdRequest$inboundSchema,
};

export const tool$nftSalesGetOrderById: ToolDefinition<typeof args> = {
  name: "NFT-sales-get-order-by-id",
  description: `Get Order

Returns Order by Id`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nftSalesGetOrderById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
