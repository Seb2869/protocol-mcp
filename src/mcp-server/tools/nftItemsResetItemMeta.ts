/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftItemsResetItemMeta } from "../../funcs/nftItemsResetItemMeta.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ResetItemMetaRequest$inboundSchema,
};

export const tool$nftItemsResetItemMeta: ToolDefinition<typeof args> = {
  name: "NFT-items-reset-item-meta",
  description: `Reset NFT metadata

Reloads NFT metadata from the source. If source not available, old metadata stays.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nftItemsResetItemMeta(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
