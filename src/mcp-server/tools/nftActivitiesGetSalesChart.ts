/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftActivitiesGetSalesChart } from "../../funcs/nftActivitiesGetSalesChart.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSalesChartRequest$inboundSchema,
};

export const tool$nftActivitiesGetSalesChart: ToolDefinition<typeof args> = {
  name: "NFT-activities-get-sales-chart",
  description: `Get sales chart

Returns list of sales by collection`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nftActivitiesGetSalesChart(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
