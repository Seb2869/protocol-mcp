/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { chartsGetVolumeChart } from "../../funcs/chartsGetVolumeChart.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetVolumeChartRequest$inboundSchema,
};

export const tool$chartsGetVolumeChart: ToolDefinition<typeof args> = {
  name: "charts-get-volume-chart",
  description:
    `Get historical data of collection volume (total sales worth) for charting purposes

Given a time period and the desired number of points, returns points which represent total worth of collection sales in the interval after that point.  The last point always has no value.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await chartsGetVolumeChart(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
