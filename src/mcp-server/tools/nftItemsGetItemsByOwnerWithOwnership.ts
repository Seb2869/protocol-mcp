/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftItemsGetItemsByOwnerWithOwnership } from "../../funcs/nftItemsGetItemsByOwnerWithOwnership.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetItemsByOwnerWithOwnershipRequest$inboundSchema,
};

export const tool$nftItemsGetItemsByOwnerWithOwnership: ToolDefinition<
  typeof args
> = {
  name: "NFT-items-get-items-by-owner-with-ownership",
  description: `Get NFT owned by user - detailed

Returns list of NFTs belong to specified user and sorted by \`last updated\` date of ownership`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nftItemsGetItemsByOwnerWithOwnership(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
