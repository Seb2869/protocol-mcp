/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftDataAndHistoricalStatisticsGetUserRankingByVolume } from "../../funcs/nftDataAndHistoricalStatisticsGetUserRankingByVolume.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetUserRankingByVolumeRequest$inboundSchema,
};

export const tool$nftDataAndHistoricalStatisticsGetUserRankingByVolume:
  ToolDefinition<typeof args> = {
    name: "NFT-data-and-historical-statistics-get-user-ranking-by-volume",
    description: `Get user volume

Users (top buyers/sellers) leaderboard. Calculated as traded worth for the period.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await nftDataAndHistoricalStatisticsGetUserRankingByVolume(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
