/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { collectionLeaderBoardGetCollectionLeaderboard } from "../../funcs/collectionLeaderBoardGetCollectionLeaderboard.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCollectionLeaderboardRequest$inboundSchema,
};

export const tool$collectionLeaderBoardGetCollectionLeaderboard: ToolDefinition<
  typeof args
> = {
  name: "collection-leader-board-get-collection-leaderboard",
  description: `Get NFT Collections leaderboard

Collections leaderboard`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await collectionLeaderBoardGetCollectionLeaderboard(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
