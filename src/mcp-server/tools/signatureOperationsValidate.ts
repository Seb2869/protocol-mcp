/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signatureOperationsValidate } from "../../funcs/signatureOperationsValidate.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.SignatureValidationForm$inboundSchema,
};

export const tool$signatureOperationsValidate: ToolDefinition<typeof args> = {
  name: "signature-operations-validate",
  description: `Check Order's signature

Checks if Order's signature is valid and returns 'true' if it so, 'false' otherwise`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signatureOperationsValidate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
