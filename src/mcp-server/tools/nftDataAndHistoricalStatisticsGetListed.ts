/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftDataAndHistoricalStatisticsGetListed } from "../../funcs/nftDataAndHistoricalStatisticsGetListed.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetListedRequest$inboundSchema,
};

export const tool$nftDataAndHistoricalStatisticsGetListed: ToolDefinition<
  typeof args
> = {
  name: "NFT-data-and-historical-statistics-get-listed",
  description: `Get NFT Collection listing stats

Get historical statistics of collection listed count`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nftDataAndHistoricalStatisticsGetListed(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
