/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftItemsGetItemById } from "../../funcs/nftItemsGetItemById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetItemByIdRequest$inboundSchema,
};

export const tool$nftItemsGetItemById: ToolDefinition<typeof args> = {
  name: "NFT-items-get-item-by-id",
  description: `Get NFT by Id

Returns NFT Item by Id`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nftItemsGetItemById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
