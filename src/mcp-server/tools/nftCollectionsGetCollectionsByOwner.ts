/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftCollectionsGetCollectionsByOwner } from "../../funcs/nftCollectionsGetCollectionsByOwner.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCollectionsByOwnerRequest$inboundSchema,
};

export const tool$nftCollectionsGetCollectionsByOwner: ToolDefinition<
  typeof args
> = {
  name: "NFT-collections-get-collections-by-owner",
  description: `Get NFT Collections owned by user

Returns list of NFT Collections belong to specified user`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nftCollectionsGetCollectionsByOwner(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
