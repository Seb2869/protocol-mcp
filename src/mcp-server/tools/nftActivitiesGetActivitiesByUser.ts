/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftActivitiesGetActivitiesByUser } from "../../funcs/nftActivitiesGetActivitiesByUser.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetActivitiesByUserRequest$inboundSchema,
};

export const tool$nftActivitiesGetActivitiesByUser: ToolDefinition<
  typeof args
> = {
  name: "NFT-activities-get-activities-by-user",
  description: `Get user Activities

Returns user's Activities (like transfers, mints, sells etc) sorted by date. This API is deprecated in favor of \`Search Activities\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nftActivitiesGetActivitiesByUser(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
