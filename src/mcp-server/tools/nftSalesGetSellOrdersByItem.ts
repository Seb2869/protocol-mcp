/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftSalesGetSellOrdersByItem } from "../../funcs/nftSalesGetSellOrdersByItem.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSellOrdersByItemRequest$inboundSchema,
};

export const tool$nftSalesGetSellOrdersByItem: ToolDefinition<typeof args> = {
  name: "NFT-sales-get-sell-orders-by-item",
  description: `Get sell Orders for NFT

Returns sell sales & transfer created for specified NFT and sorted by price in USD (cheapest first)`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nftSalesGetSellOrdersByItem(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
