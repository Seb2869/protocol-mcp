/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nftSalesGetOrderFees } from "../../funcs/nftSalesGetOrderFees.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOrderFeesRequest$inboundSchema,
};

export const tool$nftSalesGetOrderFees: ToolDefinition<typeof args> = {
  name: "NFT-sales-get-order-fees",
  description: `Get fee settings

Returns Protocol fee settings for Orders`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nftSalesGetOrderFees(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
