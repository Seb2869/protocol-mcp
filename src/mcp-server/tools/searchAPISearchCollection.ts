/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { searchAPISearchCollection } from "../../funcs/searchAPISearchCollection.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.CollectionsSearchRequest$inboundSchema,
};

export const tool$searchAPISearchCollection: ToolDefinition<typeof args> = {
  name: "search-API-search-collection",
  description: `Search NFT Collections

Advanced search returns NFT Collections satisfying provided filter`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await searchAPISearchCollection(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
